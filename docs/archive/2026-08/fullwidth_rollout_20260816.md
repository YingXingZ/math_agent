# 全章全角规整放量 — 执行结果与阻塞点（2026-08-16）

## 1. 原设想与反转
原计划：对 8014 中被 `garbage_score` 门禁 block 的 41 道（content 含全角垃圾）做全角→半角规整，
期望一次性放出为可读题。

**逐题核对 41 道原文后，前提被推翻**：这 41 道并非"单纯全角"，而是混有
**中文 OCR 乱码 + 公式符号误读**，例如：
- `S1.5#8`: `若α ~队则P ~α`（队/P 应为 β）
- `S3.5#8`: `怡，b]…f叫仰`（怡/咐/叫/仰 应为 a/b/β/β）
- `S1.2#6`: `J兑|机I` `|川一OI`（兑/机/川 乱码）
- `S2.2#7`: 整段散字乱码

`garbage_score` 只查全角拉丁/私有区；规整后该分数归零，`bulk_sync` 会据此把它们当
干净题**放出损坏内容给学生** —— 违反"绝不推乱码"红线。因此**未执行会推乱码的批量规整**。

## 2. 已安全放出的 2 道（人工核验干净）
仅放出答案干净 LaTeX、题干仅有公式误读（√→`、/`、∂→`Z`，可推断）的 2 道：
- **S6.2#4** — 答案 `z_x|_{(1,2)}=\frac{2x}{\sqrt{3+2x^2+y^2}}` 干净
- **S6.7#8** — 答案 `F_x=f_1, F_y=f_2, F_z=-af_1-bf_2` 干净

工具：`release_safe_fw.py`（白名单 + 写库前 `dirty()` 复检，绝不写入含 \ufffd/全角垃圾的字段）。
同步后 Agent：`questions` = **362 published + 22 blocked**（原 360+24，+2 放出，其余 19 仍因 content 全角垃圾被正确 block）。
`validate_agent_reports.py` **ALL VALIDATIONS PASSED**；全量 published 中 FW/替换符垃圾 = **0**。

## 3. 其余 39 道为何不能自动修复
| 路径 | 结果 |
|---|---|
| 全角规整 | ❌ 只清全角，中文乱码/公式误读仍在 → 放出去是损坏内容 |
| VLM `/solve-from-image` 重识别 | ❌ **39 道全部 `crop_image_path` 为空**，按命名约定 `extract_img/book/<sec>/p<n>_<m>_p<no>.png` 链接后 **0 道存在对应题图**（crop 仅覆盖各章前半题号，如 §2.1 只有 #1–#10，#14 无图）→ VLM 无原图可读 |

结论：这 39 道（含 3 道缺答案）既不能靠规整、也无题图给 VLM，**当前不可自动修复**。

## 4. 待定的图像源（决定能否修复这 39 道）
要修只能补源图像，三选一：
- **(A) 提供教材/答案 PDF** → 我写按题 crop 流水线，再用已就绪的 `vlm_reoc_8014.py` 批量 VLM 重识别；
- **(B) 从 IMA「高数答案OCR」知识库取原页图** → 走 18080 `/review`（需 problem→image 映射）；
- **(C) 先放着** → +2 即本轮成果，39 道保持 block 待教师/人工补。

`vlm_reoc_8014.py` 已写好（链接题图 → 调 `/solve-from-image` → 置信门禁 ≥0.5 且输出无 \ufffd/全角才写回 `vlm_recovered`，否则标 `vlm_review`），图像源就位即可一键批量跑。
