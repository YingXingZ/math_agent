DATABASE_PATH=./data/homework.db
UPLOAD_DIR=./data/uploads
DIFY_API_URL=
DIFY_API_KEY=
DIFY_WORKFLOW_ID=

# Optional: only needed by maintenance scripts that open an SSH tunnel to the
# remote VLM. Keep real values in a local .env file, never in Git.
VLM_SSH_HOST=
VLM_SSH_PORT=22
VLM_SSH_USER=
VLM_SSH_PASSWORD=
